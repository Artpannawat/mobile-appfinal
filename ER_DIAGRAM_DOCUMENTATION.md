# 🗂️ รายละเอียดโครงสร้างฐานข้อมูล (ER Diagram Tables)

ระบบจัดการห้องสมุด (Library Management System) ประกอบด้วย 3 ตารางหลัก ดังนี้:

## 1. ตารางผู้ใช้งาน (Users)
เก็บข้อมูลสมาชิกและผู้ดูแลระบบ

| ชื่อฟิลด์ (Field) | ชนิดข้อมูล (Type) | คีย์ (Key) | คำอธิบาย (Description) |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | **PK** | รหัสอ้างอิงผู้ใช้ (Primary Key) |
| `name` | String | | ชื่อ-นามสกุลของผู้ใช้งาน |
| `email` | String | | อีเมล (ใช้สำหรับเข้าสู่ระบบ) |
| `password` | String | | รหัสผ่าน (ควรเข้ารหัส) |
| `isAdmin` | Boolean | | สิทธิ์การใช้งาน (`true` = Admin, `false` = User) |

---

## 2. ตารางหนังสือ (Books)
เก็บข้อมูลทรัพยากรหนังสือในห้องสมุด

| ชื่อฟิลด์ (Field) | ชนิดข้อมูล (Type) | คีย์ (Key) | คำอธิบาย (Description) |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | **PK** | รหัสอ้างอิงหนังสือ (Primary Key) |
| `title` | String | | ชื่อเรื่องหนังสือ |
| `author` | String | | ชื่อผู้แต่ง |
| `image` | String | | URL รูปภาพหน้าปก |
| `status` | String | | สถานะหนังสือ (`available` = ว่าง, `borrowed` = ถูกยืม) |

---

## 3. ตารางรายการยืม-คืน (BorrowTransactions)
เก็บประวัติการทำรายการยืมและคืนหนังสือ

| ชื่อฟิลด์ (Field) | ชนิดข้อมูล (Type) | คีย์ (Key) | คำอธิบาย (Description) |
| :--- | :--- | :--- | :--- |
| `_id` | ObjectId | **PK** | รหัสรายการยืม-คืน (Primary Key) |
| `user_id` | ObjectId | **FK** | อ้างอิงตาราง **Users** (ใครยืม?) |
| `book_id` | ObjectId | **FK** | อ้างอิงตาราง **Books** (ยืมเล่มไหน?) |
| `borrowDate` | DateTime | | วันที่และเวลาที่ทำการยืม |
| `returnDate` | DateTime | | วันที่และเวลาที่นำมาคืน (ค่าว่าง = ยังไม่คืน) |

---

## 🔗 ความสัมพันธ์ (Relationships)
ข้อมูลมีความสัมพันธ์กันในรูปแบบ **One-to-Many (1:N)** ดังนี้:

1.  **Users 1 : N BorrowTransactions**
    *   ผู้ใช้งาน 1 คน สามารถทำรายการยืมหนังสือได้หลายครั้ง
    *   *("Users ทำการยืม ใน BorrowTransactions")*

2.  **Books 1 : N BorrowTransactions**
    *   หนังสือ 1 เล่ม สามารถถูกยืมได้หลายครั้ง (ในช่วงเวลาที่ต่างกัน)
    *   *("Books ถูกยืมในรายการ BorrowTransactions")*
